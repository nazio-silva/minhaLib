package com.utils;

public class SendEvent {

    /*public static void emit(ReactContext reactContext, Stringv eentName, @Nullable WritableMap writableMap)
    {
        reactContext
                .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class)
                .emit(eventName, writableMap);
    }
    */

    public static void emit() {
        System.out.println("Teste emit...");
    }
}
